<!doctype html>
<html lang="en-US">
<head>
    <meta charset="UTF-8">
    <title>{{ title }}</title>
    <link rel="stylesheet" type="text/css" href="/css/main.css" media="all"/>
    <link rel="stylesheet" type="text/css" href="/semantic/semantic.min.css" media="all"/>
    {#<link rel="stylesheet" type="text/css" href="/semantic/components/icon.min.css" media="all"/>#}
    <script src="https://code.jquery.com/jquery-3.1.1.min.js"
            integrity="sha256-hVVnYaiADRTO2PzUGmuLJr8BLUSjGIZsDYGmIJLv2b8="
            crossorigin="anonymous"></script>
    <script src="/semantic/semantic.min.js"></script>
</head>
<body>
    <div id="content">
        <div id="content-pad">
            <div id="menu">
                {% for pageName, url in _pages %}
                    {% if (url == _currentPage ) %}
                        <a href="{{ url }}" class="nav-current">{{ pageName }}</a>
                    {% else %}
                        <a href="{{ url }}">{{ pageName }}</a>
                    {% endif %}
                    {% if not loop.last %} | {% endif %}
                {% endfor %}
                <span style="float: right;">{{ _user.name }} {{ _budget ? '(' ~ _budget.name ~ ')' : '' }}</span>
            </div>
        {% block content %}
        {% endblock %}
        </div>
    </div>
</body>
</html>