{% extends _layout %}

{% block content %}
    {% include '_styles.twig' %}
    {% if(authorised == false) %}
        <div class="ui middle aligned center aligned grid" id="login-form">
            <div class="column">
                <h2 class="ui teal image header">
                    <i class="lightbulb icon"></i>
                    <div class="content">
                        Войдите в систему
                    </div>
                </h2>
                <form class="ui large form {{ message ? 'error' : '' }}" action="/auth" method="post">
                    <input type="hidden" name="submit" value="1"/>
                    <div class="ui stacked segment">
                        <div class="field">
                            <div class="ui left icon input">
                                <i class="mail icon"></i>
                                <input type="text" name="email" value="{{ email }}" placeholder="E-mail address">
                            </div>
                        </div>
                        <div class="field">
                            <div class="ui left icon input">
                                <i class="lock icon"></i>
                                <input type="password" name="password" placeholder="Password">
                            </div>
                        </div>
                        <input class="ui fluid large teal submit button" type="submit" value="Войти"/>
                    </div>

                    <div class="ui error message">{{ message }}</div>
                </form>

                <div class="ui message">
                    Если у тебе еще нет аккаунта <a href="/auth-signup">Зарегистрирйся</a><br>
                    Если забыл пароль ты можешь <a href="/auth/reset">Восстановить пароль</a>
                </div>
            </div>
        </div>
    {% else %}
        Ты успешно авторизован!
    {% endif %}
{% endblock %}